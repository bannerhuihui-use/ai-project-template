# 开发环境（npm run dev 自动加载，可提交仓库）
# 个人覆盖：新建 .env.local 或 .env.development.local（已在 .gitignore）

# 接口前缀：开发期走 Vite 代理，填 /api 即可
VITE_API_BASE_URL=/api

# Vite dev server 代理目标（仅开发有效，不会打进生产包）
VITE_PROXY_TARGET=http://localhost:8080

# token 存储：local（关浏览器仍在）| session（关标签页失效）
VITE_TOKEN_STORAGE=local

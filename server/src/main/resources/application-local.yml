# ===== 本地环境（默认）=====
# 用到某个中间件时：1) 在 pom.xml 取消对应依赖注释 2) 在此取消对应配置注释 3) 在 .env 填写值
# 关系型数据库（单数据源）配置已统一在 application.yml，值来自 .env。

spring:
  # ---- 邮箱 ----
  # mail:
  #   host: ${MAIL_HOST}
  #   port: ${MAIL_PORT:465}
  #   username: ${MAIL_USERNAME}
  #   password: ${MAIL_PASSWORD}
  #   properties:
  #     mail.smtp.auth: true
  #     mail.smtp.ssl.enable: true

# ===== 第三方服务（自定义配置，用 @ConfigurationProperties 或 @Value 读取）=====

# 阿里云 OSS
# oss:
#   endpoint: ${OSS_ENDPOINT}
#   access-key-id: ${OSS_ACCESS_KEY_ID}
#   access-key-secret: ${OSS_ACCESS_KEY_SECRET}
#   bucket: ${OSS_BUCKET}

# 火山引擎 方舟大模型
# volcengine:
#   ark:
#     api-key: ${VOLC_ARK_API_KEY}
#     model: ${VOLC_ARK_MODEL}
#     base-url: ${VOLC_ARK_BASE_URL:https://ark.cn-beijing.volces.com/api/v3}

# 短信
# sms:
#   access-key-id: ${SMS_ACCESS_KEY_ID}
#   access-key-secret: ${SMS_ACCESS_KEY_SECRET}
#   sign-name: ${SMS_SIGN_NAME}
#   template-code: ${SMS_TEMPLATE_CODE}
